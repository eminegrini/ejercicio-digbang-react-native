//este archivo se crea para señalizar la ruta donde se encuentran los fonts para poder linkearlos a android

module.exports = {
  project: {
    ios: {},
    android: {}, // grouped into "project"
  },
  assets: ['./src/assets/fonts/'], // stays the same
};
